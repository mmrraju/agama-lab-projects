Flow org.gluu.agama.user.registration
     Basepath ""
// Create json variable for UI feedback.
uiFeedback = {}
// Iterate x times max
max = Repeat 3 times max
     // Retrieve user profile data from UI
     profile = RRF "profile.ftlh" uiFeedback